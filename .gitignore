# OS
.DS_Store
Thumbs.db

# User projects (each has its own git repo)
*.aDNA/

# Obsidian per-device (don't sync across clones)
.adna/.obsidian/workspace.json
.adna/.obsidian/workspace-mobile.json
.adna/.obsidian/graph.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Note: plugin binaries and themes ARE tracked so the vault works on clone.
# Run ./setup.sh --force to update all plugins to latest versions.

# Environment
.env
.env.local

# Claude Code / MCP (local credentials — never commit)
.mcp.json
.claude/

# Local working context (not part of upstream)
_meta/
